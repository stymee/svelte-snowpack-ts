/* src\components\Test1.svelte generated by Svelte v3.31.2 */
import {
	SvelteComponent,
	append,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	set_data,
	text
} from "../../_snowpack/pkg/svelte/internal.js";

import dayjs from "../../_snowpack/pkg/dayjs.js";
import { onMount } from "../../_snowpack/pkg/svelte.js";

function create_fragment(ctx) {
	let h4;
	let t0;
	let t1;

	return {
		c() {
			h4 = element("h4");
			t0 = text("Test111! ");
			t1 = text(/*nowTime*/ ctx[0]);
		},
		m(target, anchor) {
			insert(target, h4, anchor);
			append(h4, t0);
			append(h4, t1);
		},
		p(ctx, [dirty]) {
			if (dirty & /*nowTime*/ 1) set_data(t1, /*nowTime*/ ctx[0]);
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(h4);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { location } = $$props;
	let nowTime = "";

	onMount(() => {
		console.log("Home location", location);

		const interval = setInterval(
			() => {
				$$invalidate(0, nowTime = dayjs(Date.now()).format("ddd M/D/YY h:mm:ss.SSS a"));
			},
			10
		);

		return () => {
			clearInterval(interval);
		};
	});

	$$self.$$set = $$props => {
		if ("location" in $$props) $$invalidate(1, location = $$props.location);
	};

	return [nowTime, location];
}

class Test1 extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { location: 1 });
	}
}

export default Test1;